<div class="subnav tabs is-centered">
  <ul>
    <% subnavigables(@item[:subnavigation][:bar]).map { |e| e[:subnavigation] }.each do |nav| %>
    <li class="<%= 'is-active' if @selected == nav[:identifier] %>">
      <a href="<%= localized_path("/#{nav[:bar]}/#{nav[:identifier]}") %>">
        <%= t(nav[:name]) %>
      </a>
    </li>
    <% end %>
  </ul>
</div>
